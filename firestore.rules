rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The old Firestore-backed voting prototype is now archived.
    // Keep client access closed unless a future implementation
    // intentionally reopens a narrower document path.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
